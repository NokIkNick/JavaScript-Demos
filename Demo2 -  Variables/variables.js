"use strict";

//Declaring variables and assigning values:
let admin;
let name = "John";
admin = name;
alert(admin);

//Naming variables:
let planetName;
let userName;

//Constants with capital or not:
const BIRTHDAY = "18.04.1982"; //Should be capital since its known before runtime.
const age = 0; //Age shouldn't be capital since its being calculated by a seperate method in runtime.